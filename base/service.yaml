apiVersion: v1
kind: Service
metadata:
  name: mealie-mcp
  namespace: mealie-mcp
  labels:
    app: mealie-mcp
spec:
  type: ClusterIP
  selector:
    app: mealie-mcp
  ports:
  - name: mcp
    port: 8765
    targetPort: mcp
    protocol: TCP
