apiVersion: v1
kind: Secret
metadata:
  name: mealie-mcp-harbor-pull
  namespace: mealie-mcp
type: kubernetes.io/dockerconfigjson
stringData:
  # TODO: Replace with actual Harbor pull secret config
  .dockerconfigjson: |
    {"auths":{}}
